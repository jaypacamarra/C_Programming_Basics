#include <stdio.h>

/* Using register, extern and static
 * scope = where the identifier can be referenced in a program
 * linkage = determines for a multi-sourceifile program, whether the identifier is known only in the current source file or in any source file with proper declerations
 *  
*/

int main(void)
{
    
}
